// (1) http://tools.android.com/tech-docs/new-build-system/gradle-experimental
apply plugin: 'com.android.model.application' // (1)

model {
    android {
        compileSdkVersion = 22
        buildToolsVersion = "22.0.1"

        defaultConfig.with {
            applicationId = "com.fyrecloud.andrinoime"
            minSdkVersion.apiLevel = 11
            targetSdkVersion.apiLevel = 22
            versionCode = 1
            versionName = "1.0"
        }

    }

     android.ndk {
         moduleName = "hello-tommy"
         ldLibs += "log"
         //stl = "stlport_static", vector, but not memory
         //stl = "gabi++_static", memory, but not vector
         stl = "gnustl_static"
     }

}


